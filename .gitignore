# ==========================================================================
# Installer-managed plugin checkouts
# Declared in manifests/plugins.tsv, cloned by install/20-plugins.sh.
# Never tracked here: they are upstream repos, not our source.
# ==========================================================================
.oh-my-zsh/
omz-custom/plugins/
omz-custom/themes/

# ==========================================================================
# Machine-local overrides & secrets  (sourced last by .zshrc if present)
# ==========================================================================
local.zsh
secrets.zsh
p10k.local.zsh
.env
.local.zsh
.local/
.note/

# ==========================================================================
# Completion dumps, caches and transient state
# ==========================================================================
.zcompdump
.zcompdump*
.zcompdump-*
.zcompdump.zwc
.zsh-update
.zsh_sessions/
.p10k.zsh.bak
.p10k.zsh.zwc
history
.zsh_history

# ==========================================================================
# OS noise
# ==========================================================================
.DS_Store
